<UserControl
    x:Class="SwellSSH.Terminal.TerminalView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    Loaded="UserControl_Loaded"
    Unloaded="UserControl_Unloaded"
    GettingFocus="UserControl_GettingFocus"
    SizeChanged="UserControl_SizeChanged"
    PointerPressed="UserControl_PointerPressed"
    PointerMoved="UserControl_PointerMoved"
    PointerReleased="UserControl_PointerReleased"
    IsTabStop="True"
    AllowFocusOnInteraction="True">
    
    <!-- We need a grid to hold the canvas so we can set a background color -->
    <Grid x:Name="RootGrid" Background="#FF0C0C0C">
        <!-- Win2D CanvasControl for immediate mode 2D rendering -->
        <canvas:CanvasControl x:Name="Canvas"
                              Draw="Canvas_Draw"
                              CreateResources="Canvas_CreateResources"
                              ClearColor="#FF0C0C0C" />
    </Grid>
</UserControl>
